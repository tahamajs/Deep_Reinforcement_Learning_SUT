# CA15 — Curriculum Assignment 15

## Overview

This folder contains the README for CA15. The purpose of CA15 is to provide a concise assignment specification, references, and usage notes consistent with other CAs in this repository. No code or data is included in this directory; only documentation.

## Learning Goals

- Understand the core algorithm described in the assignment brief.
- Implement a research-grade, import-safe Python module in `src/` (if requested later).
- Produce a Jupyter notebook for experiments and visualizations.

## Folder Structure (expected)

- `src/` — library-style modules (import-safe).
- `notebooks/` — demonstration and visualization notebooks.
- `configs/` — example YAML/config files.
- `tests/` — unit tests for core pieces.

## Quick Start

This README is intentionally minimal. If you want me to scaffold code (e.g. `src/config.py`, `src/model.py`) or a notebook template for CA15, tell me and I will add it.

## References

- [Relevant paper or resource placeholder]

## Notes

Follow repository conventions: keep modules import-safe, use type hints, and place all runnable training loops inside notebooks or scripts guarded by `if __name__ == "__main__"`.











