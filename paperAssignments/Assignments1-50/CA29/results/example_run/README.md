Example run folder used for demo plotting and report illustrations.

Files:
- metrics.csv: Example evaluation summaries with columns `step,avg_reward,std_reward`.

How to use:
1. Open `notebooks/experiment_template.ipynb`.
2. Set `run_dir = 'results/example_run'` in the plotting cells.
3. Run the plotting cells to reproduce example learning curves shown in the report.