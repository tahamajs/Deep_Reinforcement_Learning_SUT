This folder contains `demo.ipynb` (a short demo to visualize placeholder plots).

If you don't want to run the notebook, you can generate the placeholder figures used by the report by running:

```bash
python scripts/generate_placeholder_figs.py
```

Opening the notebook in JupyterLab or VSCode will show the plots and a short walk-through for diagnostics.