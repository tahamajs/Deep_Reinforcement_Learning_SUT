# CA_extra_versions/ – Historical Copies of Extra Sessions

Two parallel trees hold older variants of the extra-session notebooks:
- `clean/` – sanitized versions with minor fixes; includes `README.txt` with details.
- `pre_clean/` – raw originals before cleanup.

Use these if you need to reproduce earlier results or compare against the cleaned notebooks in `CA_extra/`.
